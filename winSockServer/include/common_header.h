#ifndef COMMON_HEADER_H_
#define COMMON_HEADER_H_
#include <winsock2.h>
#include <string>
#include <functional>
#include <thread>
#include <vector>

enum class ServerStatus { ON = 0, OFF };

enum class MODE { WRITE = 0, READ = 1 };

#endif //COMMON_HEADER_H_